# Exercise 4: Neural Network

In this exercise, you need to complete both [`nn/Makefile`](./nn/Makefile) and [`Makefile`](./Makefile) so that the NN library and the test program can be compiled correctly.
